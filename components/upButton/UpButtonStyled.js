import styled from "styled-components";
import {motion} from "framer-motion";

export const UpButtonStyled = styled(motion.div)`
  position: fixed;
  bottom: 12rem;
  right: 3em;
  width: 5rem;
  height: 5rem;
  cursor: pointer;
  z-index: 9;
`